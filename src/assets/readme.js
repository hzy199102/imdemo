/**
 * 待完成的功能列表：
 * 1.切换窗口事件监听
 */

/**
 * npm info element-ui 查看element-ui可安装版本
 * http://www.bejson.com/convert/ox2str/ 字符串转16进制
 *
 */

/**
 * js调用外部程序exe方式调研
 *
 */

/**
 * 浏览器适配调研，@舒盺
 *
 */

/**
 * vue中，页面创建一个弹出框，在弹出框关闭之后，弹出框嵌套的vue组件是否会走destroyed方法？是否会自动销毁？
 * 答案是不会，所以应该主动触发_this.$destroy()，并且在里面自动除去注册的元素事件，或者定时器等等
 *
 * vue中，router:A-->B,A的destroyed先触发还是B的created先触发
 * B的created先触发
 *
 *
 *
 *
 */
